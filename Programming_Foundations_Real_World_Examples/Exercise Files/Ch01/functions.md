# Functions

A routine or procedure that may or may not taken in processes and may or may not return a value.

Why? or Should or should not? Used for commonly used routines.

## Scope

Looks for local scope first and global second. Creates the local variable at runtime as to not override the global variable.
